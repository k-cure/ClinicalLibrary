CREATE OR REPLACE PROCEDURE quality_check_tot_cnt()
BEGIN
    DECLARE RULE_ID_VAL VARCHAR(30);
    DECLARE TB_NM_VAL VARCHAR(50);
    DECLARE COL_NM_VAL VARCHAR(50);
    DECLARE TOT_CNT_VAL VARCHAR(50);
    DECLARE ERR_CNT_VAL VARCHAR(50);
    DECLARE ERR_VALUE_VAL TEXT;

    -- 테이블 생성
    CREATE TABLE IF NOT EXISTS result_quality_check (
        RULE_ID VARCHAR(30),
        TB_NM VARCHAR(50),
        COL_NM VARCHAR(50),
        TOT_CNT VARCHAR(50),
        ERR_CNT VARCHAR(50),
        ERR_VALUE TEXT
    );

   CREATE TABLE IF NOT EXISTS quality_check_error_log (
        SEQ SERIAL PRIMARY KEY,
        CHK_ID VARCHAR(30),
        CHK_TIME TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        ERROR_MESSAGE TEXT
    );

    DELETE FROM result_quality_check WHERE 1=1;

    COMMIT;
    BEGIN
        -- 오류 발생 시 에러 로그에 기록
        DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
        BEGIN
            INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
            VALUES (RULE_ID_VAL, ERROR_MESSAGE());
        END;

        -- 쿼리 결과를 변수에 저장
        SELECT 'ID3001' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
 
        INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

        -- 결과를 테이블에 삽입
        INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
        VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
    END;
    COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3002' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3003' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3004' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3005' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'GENDER_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE GENDER_CONCEPT_ID IS NOT NULL AND LENGTH(GENDER_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3006' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'GENDER_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3007' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'YEAR_OF_BIRTH' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE YEAR_OF_BIRTH IS NOT NULL AND LENGTH(YEAR_OF_BIRTH)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3008' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'YEAR_OF_BIRTH' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3009' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'YEAR_OF_BIRTH' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE YEAR_OF_BIRTH IS NOT NULL AND LENGTH(YEAR_OF_BIRTH)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3010' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'MONTH_OF_BIRTH' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE MONTH_OF_BIRTH IS NOT NULL AND LENGTH(MONTH_OF_BIRTH)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3011' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'DAY_OF_BIRTH' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE DAY_OF_BIRTH IS NOT NULL AND LENGTH(DAY_OF_BIRTH)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3013' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'RACE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE RACE_CONCEPT_ID IS NOT NULL AND LENGTH(RACE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3014' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'RACE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3015' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'ETHNICITY_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE ETHNICITY_CONCEPT_ID IS NOT NULL AND LENGTH(ETHNICITY_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3016' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'ETHNICITY_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3017' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'LOCATION_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE LOCATION_ID IS NOT NULL AND LENGTH(LOCATION_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;





BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3022' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'GENDER_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE GENDER_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(GENDER_SOURCE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3024' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'RACE_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE RACE_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(RACE_SOURCE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;




BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3028' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3029' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3030' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3031' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
WHERE OBSERVATION_PERIOD_START_DATE IS NOT NULL AND LENGTH(OBSERVATION_PERIOD_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3032' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3033' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
WHERE OBSERVATION_PERIOD_START_DATE IS NOT NULL AND LENGTH(OBSERVATION_PERIOD_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3034' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
WHERE OBSERVATION_PERIOD_START_DATE IS NOT NULL AND LENGTH(OBSERVATION_PERIOD_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3035' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
WHERE OBSERVATION_PERIOD_END_DATE IS NOT NULL AND LENGTH(OBSERVATION_PERIOD_END_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3036' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3037' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3038' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'PERIOD_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
WHERE PERIOD_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(PERIOD_TYPE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3039' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'PERIOD_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3041' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3042' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3043' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3044' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_CONCEPT_ID IS NOT NULL AND LENGTH(VISIT_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3045' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3046' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_START_DATE IS NOT NULL AND LENGTH(VISIT_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3047' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3048' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_START_DATE IS NOT NULL AND LENGTH(VISIT_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3049' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_START_DATE IS NOT NULL AND LENGTH(VISIT_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3050' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_START_DATETIME IS NOT NULL AND LENGTH(VISIT_START_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3051' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_END_DATE IS NOT NULL AND LENGTH(VISIT_END_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3052' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3053' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3054' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_END_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_END_DATETIME IS NOT NULL AND LENGTH(VISIT_END_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3055' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(VISIT_TYPE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3056' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;




BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3060' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(VISIT_SOURCE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3061' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'ADMITTED_FROM_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE ADMITTED_FROM_CONCEPT_ID IS NOT NULL AND LENGTH(ADMITTED_FROM_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3063' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'DISCHARGED_TO_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE DISCHARGED_TO_CONCEPT_ID IS NOT NULL AND LENGTH(DISCHARGED_TO_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;




BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3067' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3068' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3069' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3070' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_CONCEPT_ID IS NOT NULL AND LENGTH(VISIT_DETAIL_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3071' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3072' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_START_DATE IS NOT NULL AND LENGTH(VISIT_DETAIL_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3073' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3074' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3075' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3076' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_START_DATETIME IS NOT NULL AND LENGTH(VISIT_DETAIL_START_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3077' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_END_DATE IS NOT NULL AND LENGTH(VISIT_DETAIL_END_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3078' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3079' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3080' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3081' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_END_DATETIME IS NOT NULL AND LENGTH(VISIT_DETAIL_END_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3082' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(VISIT_DETAIL_TYPE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3083' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;




BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3087' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(VISIT_DETAIL_SOURCE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3089' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'ADMITTED_FROM_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE ADMITTED_FROM_CONCEPT_ID IS NOT NULL AND LENGTH(ADMITTED_FROM_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3091' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'DISCHARGED_TO_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE DISCHARGED_TO_CONCEPT_ID IS NOT NULL AND LENGTH(DISCHARGED_TO_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;



BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3094' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(VISIT_OCCURRENCE_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3095' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3097' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3098' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3099' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3100' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_CONCEPT_ID IS NOT NULL AND LENGTH(CONDITION_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3101' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3102' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_CONCEPT_ID IS NOT NULL AND LENGTH(CONDITION_CONCEPT_ID)>0
AND CONDITION_CONCEPT_ID IN (
SELECT DESCENDANT_CONCEPT_ID FROM YourSchemaName.CONCEPT_ANCESTOR WHERE ANCESTOR_CONCEPT_ID IN (45771080,4216771,3655355,4090861,4057948)
) 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3103' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_CONCEPT_ID IS NOT NULL AND LENGTH(CONDITION_CONCEPT_ID)>0
AND CONDITION_CONCEPT_ID IN (
SELECT DESCENDANT_CONCEPT_ID FROM YourSchemaName.CONCEPT_ANCESTOR WHERE ANCESTOR_CONCEPT_ID IN (4129486,4095793,443343,4268895,4056331)
) 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3104' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_START_DATE IS NOT NULL AND LENGTH(CONDITION_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3105' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3106' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_START_DATE IS NOT NULL AND LENGTH(CONDITION_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3107' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_START_DATE IS NOT NULL AND LENGTH(CONDITION_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3108' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_START_DATE IS NOT NULL AND LENGTH(CONDITION_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3109' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_START_DATETIME IS NOT NULL AND LENGTH(CONDITION_START_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;



BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3112' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(CONDITION_TYPE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3113' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3114' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_STATUS_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_STATUS_CONCEPT_ID IS NOT NULL AND LENGTH(CONDITION_STATUS_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;



BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3117' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(VISIT_OCCURRENCE_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;






BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3123' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3124' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3125' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3126' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_CONCEPT_ID IS NOT NULL AND LENGTH(DRUG_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3127' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3128' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_EXPOSURE_START_DATE IS NOT NULL AND LENGTH(DRUG_EXPOSURE_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3129' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3130' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_EXPOSURE_START_DATE IS NOT NULL AND LENGTH(DRUG_EXPOSURE_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3131' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_EXPOSURE_START_DATE IS NOT NULL AND LENGTH(DRUG_EXPOSURE_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3132' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_EXPOSURE_START_DATE IS NOT NULL AND LENGTH(DRUG_EXPOSURE_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3133' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_EXPOSURE_START_DATETIME IS NOT NULL AND LENGTH(DRUG_EXPOSURE_START_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3134' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_EXPOSURE_END_DATE IS NOT NULL AND LENGTH(DRUG_EXPOSURE_END_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3135' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3136' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;



BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3139' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(DRUG_TYPE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3140' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;



BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3143' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'QUANTITY' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE QUANTITY IS NOT NULL AND LENGTH(QUANTITY)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3144' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DAYS_SUPPLY' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DAYS_SUPPLY IS NOT NULL AND LENGTH(DAYS_SUPPLY)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3146' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'ROUTE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE ROUTE_CONCEPT_ID IS NOT NULL AND LENGTH(ROUTE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;



BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3149' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(VISIT_OCCURRENCE_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;



BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3152' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(DRUG_SOURCE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;




BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3156' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3157' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3158' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3159' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_CONCEPT_ID IS NOT NULL AND LENGTH(PROCEDURE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3160' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3161' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_DATE IS NOT NULL AND LENGTH(PROCEDURE_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3162' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3163' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_DATE IS NOT NULL AND LENGTH(PROCEDURE_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3164' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_DATE IS NOT NULL AND LENGTH(PROCEDURE_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3165' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_DATE IS NOT NULL AND LENGTH(PROCEDURE_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3166' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_DATETIME IS NOT NULL AND LENGTH(PROCEDURE_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3167' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_END_DATE IS NOT NULL AND LENGTH(PROCEDURE_END_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3168' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_END_DATE IS NOT NULL AND LENGTH(PROCEDURE_END_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3169' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_END_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_END_DATETIME IS NOT NULL AND LENGTH(PROCEDURE_END_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3170' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(PROCEDURE_TYPE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3171' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3172' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'MODIFIER_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE MODIFIER_CONCEPT_ID IS NOT NULL AND LENGTH(MODIFIER_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3173' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'QUANTITY' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE QUANTITY IS NOT NULL AND LENGTH(QUANTITY)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3175' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(VISIT_OCCURRENCE_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;






BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3181' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3182' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3183' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3184' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_CONCEPT_ID IS NOT NULL AND LENGTH(DEVICE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3185' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3186' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_EXPOSURE_START_DATE IS NOT NULL AND LENGTH(DEVICE_EXPOSURE_START_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3187' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3188' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3189' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3190' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_EXPOSURE_START_DATETIME IS NOT NULL AND LENGTH(DEVICE_EXPOSURE_START_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3191' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_EXPOSURE_END_DATE IS NOT NULL AND LENGTH(DEVICE_EXPOSURE_END_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3192' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_EXPOSURE_END_DATE IS NOT NULL AND LENGTH(DEVICE_EXPOSURE_END_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3193' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_END_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_EXPOSURE_END_DATETIME IS NOT NULL AND LENGTH(DEVICE_EXPOSURE_END_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3194' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(DEVICE_TYPE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3195' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;



BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3198' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'QUANTITY' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE QUANTITY IS NOT NULL AND LENGTH(QUANTITY)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3200' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(VISIT_OCCURRENCE_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;



BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3203' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(DEVICE_SOURCE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3204' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'UNIT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE UNIT_CONCEPT_ID IS NOT NULL AND LENGTH(UNIT_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3206' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'UNIT_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE UNIT_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(UNIT_SOURCE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3208' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3209' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3210' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3211' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE MEASUREMENT_CONCEPT_ID IS NOT NULL AND LENGTH(MEASUREMENT_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3212' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3213' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE MEASUREMENT_DATE IS NOT NULL AND LENGTH(MEASUREMENT_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3214' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3215' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE MEASUREMENT_DATE IS NOT NULL AND LENGTH(MEASUREMENT_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3216' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE MEASUREMENT_DATE IS NOT NULL AND LENGTH(MEASUREMENT_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3217' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE MEASUREMENT_DATE IS NOT NULL AND LENGTH(MEASUREMENT_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3218' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE MEASUREMENT_DATETIME IS NOT NULL AND LENGTH(MEASUREMENT_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3220' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE MEASUREMENT_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(MEASUREMENT_TYPE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3221' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3222' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'OPERATOR_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE OPERATOR_CONCEPT_ID IS NOT NULL AND LENGTH(OPERATOR_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3223' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'VALUE_AS_NUMBER' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE VALUE_AS_NUMBER IS NOT NULL AND LENGTH(VALUE_AS_NUMBER)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3224' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'VALUE_AS_NUMBER' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3225' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'VALUE_AS_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE VALUE_AS_CONCEPT_ID IS NOT NULL AND LENGTH(VALUE_AS_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3226' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'UNIT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE UNIT_CONCEPT_ID IS NOT NULL AND LENGTH(UNIT_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3227' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'RANGE_LOW' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE RANGE_LOW IS NOT NULL AND LENGTH(RANGE_LOW)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3228' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'RANGE_HIGH' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE RANGE_HIGH IS NOT NULL AND LENGTH(RANGE_HIGH)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3230' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(VISIT_OCCURRENCE_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;





BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3235' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'UNIT_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE UNIT_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(UNIT_SOURCE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;





BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3240' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3241' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3242' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3243' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_CONCEPT_ID IS NOT NULL AND LENGTH(OBSERVATION_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3244' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3245' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_DATE IS NOT NULL AND LENGTH(OBSERVATION_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3246' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3247' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_DATE IS NOT NULL AND LENGTH(OBSERVATION_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3248' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_DATE IS NOT NULL AND LENGTH(OBSERVATION_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3249' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_DATE IS NOT NULL AND LENGTH(OBSERVATION_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3250' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_DATETIME IS NOT NULL AND LENGTH(OBSERVATION_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3251' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(OBSERVATION_TYPE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3252' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3253' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'VALUE_AS_NUMBER' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE VALUE_AS_NUMBER IS NOT NULL AND LENGTH(VALUE_AS_NUMBER)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3254' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'VALUE_AS_NUMBER' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3256' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'VALUE_AS_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE VALUE_AS_CONCEPT_ID IS NOT NULL AND LENGTH(VALUE_AS_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3257' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'QUALIFIER_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE QUALIFIER_CONCEPT_ID IS NOT NULL AND LENGTH(QUALIFIER_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3258' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'UNIT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE UNIT_CONCEPT_ID IS NOT NULL AND LENGTH(UNIT_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3260' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(VISIT_OCCURRENCE_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;



BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3263' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(OBSERVATION_SOURCE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;






BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3269' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3270' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3271' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3272' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'DEATH_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH
WHERE DEATH_DATE IS NOT NULL AND LENGTH(DEATH_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3273' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'DEATH_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3275' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'DEATH_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH
WHERE DEATH_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(DEATH_TYPE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3276' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'CAUSE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH
WHERE CAUSE_CONCEPT_ID IS NOT NULL AND LENGTH(CAUSE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;




BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3280' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3281' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3282' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3283' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE NOTE_DATE IS NOT NULL AND LENGTH(NOTE_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3284' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3285' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3286' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3287' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE NOTE_DATETIME IS NOT NULL AND LENGTH(NOTE_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3288' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE NOTE_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(NOTE_TYPE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3289' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3290' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_CLASS_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE NOTE_CLASS_CONCEPT_ID IS NOT NULL AND LENGTH(NOTE_CLASS_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3291' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_CLASS_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;



BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3294' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_TEXT' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3295' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'ENCODING_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE ENCODING_CONCEPT_ID IS NOT NULL AND LENGTH(ENCODING_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3296' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'ENCODING_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3297' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'LANGUAGE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE LANGUAGE_CONCEPT_ID IS NOT NULL AND LENGTH(LANGUAGE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3298' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'LANGUAGE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3300' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(VISIT_OCCURRENCE_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;






BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3306' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_NLP_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3307' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
WHERE NOTE_ID IS NOT NULL AND LENGTH(NOTE_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3308' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3309' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'SECTION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
WHERE SECTION_CONCEPT_ID IS NOT NULL AND LENGTH(SECTION_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;




BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3313' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'LEXICAL_VARIANT' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3314' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_NLP_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
WHERE NOTE_NLP_CONCEPT_ID IS NOT NULL AND LENGTH(NOTE_NLP_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3315' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_NLP_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
WHERE NOTE_NLP_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(NOTE_NLP_SOURCE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;


BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3317' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NLP_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
WHERE NLP_DATE IS NOT NULL AND LENGTH(NLP_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3318' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NLP_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3319' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NLP_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
WHERE NLP_DATETIME IS NOT NULL AND LENGTH(NLP_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;





BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3324' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3325' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE PERSON_ID IS NOT NULL AND LENGTH(PERSON_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3326' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3327' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE SPECIMEN_CONCEPT_ID IS NOT NULL AND LENGTH(SPECIMEN_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3328' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3329' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE SPECIMEN_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(SPECIMEN_TYPE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3330' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3331' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE SPECIMEN_DATE IS NOT NULL AND LENGTH(SPECIMEN_DATE)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3332' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3333' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3334' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE SPECIMEN_DATETIME IS NOT NULL AND LENGTH(SPECIMEN_DATETIME)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3335' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'QUANTITY' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE QUANTITY IS NOT NULL AND LENGTH(QUANTITY)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3336' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'UNIT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE UNIT_CONCEPT_ID IS NOT NULL AND LENGTH(UNIT_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3337' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'ANATOMIC_SITE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE ANATOMIC_SITE_CONCEPT_ID IS NOT NULL AND LENGTH(ANATOMIC_SITE_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;

BEGIN
    -- 오류 발생 시 에러 로그에 기록
    DECLARE CONTINUE HANDLER FOR SQLEXCEPTION
    BEGIN
        INSERT INTO quality_check_error_log (CHK_ID, ERROR_MESSAGE)
        VALUES (RULE_ID_VAL, ERROR_MESSAGE());
    END;

    -- 쿼리 결과를 변수에 저장
    SELECT 'ID3338' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'DISEASE_STATUS_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE DISEASE_STATUS_CONCEPT_ID IS NOT NULL AND LENGTH(DISEASE_STATUS_CONCEPT_ID)>0 
    INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;

    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
END;
COMMIT;
END;


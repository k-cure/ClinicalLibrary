CREATE PROCEDURE quality_check_err_val
    AS
BEGIN
    -- 프로시저 본문
    -- 변수 선언
    DECLARE @RULE_ID_VAL VARCHAR(30);
    DECLARE @TB_NM_VAL VARCHAR(50);
    DECLARE @COL_NM_VAL VARCHAR(50);
    DECLARE @TOT_CNT_VAL VARCHAR(50);
    DECLARE @ERR_CNT_VAL VARCHAR(50);
    DECLARE @ERR_VALUE_VAL VARCHAR(4000);
BEGIN
    -- 테이블 생성
    IF OBJECT_ID('dbo.result_quality_check', 'U') IS NULL
    BEGIN
        CREATE TABLE dbo.result_quality_check (
            RULE_ID VARCHAR(30),
            TB_NM VARCHAR(50),
            COL_NM VARCHAR(50),
            TOT_CNT VARCHAR(50),
            ERR_CNT VARCHAR(50),
            ERR_VALUE TEXT
        );
    END;
    IF OBJECT_ID('dbo.quality_check_error_log', 'U') IS NULL
    BEGIN
        CREATE TABLE dbo.quality_check_error_log (
            SEQ INT IDENTITY(1,1) PRIMARY KEY,
            CHK_ID VARCHAR(30),
            CHK_TIME DATETIME DEFAULT GETDATE(),
            ERROR_MESSAGE TEXT
        );
    END;
END;
BEGIN
    UPDATE RESULT_QUALITY_CHECK SET ERR_VALUE = NULL WHERE 1=1;
END;
BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3001' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
    FROM YourSchemaName.PERSON A
    WHERE  PERSON_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3002' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.PERSON A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.PERSON_ID = A.PERSON_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3003' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.PERSON A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.PROCEDURE_OCCURRENCE B WHERE B.PERSON_ID = A.PERSON_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3004' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.PERSON A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND (CAST(A.YEAR_OF_BIRTH AS VARCHAR) + '-' 
      + RIGHT('00' + CAST(A.MONTH_OF_BIRTH AS VARCHAR), 2) + '-' 
      + RIGHT('00' + CAST(A.DAY_OF_BIRTH AS VARCHAR), 2)) < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(GENDER_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3005' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'GENDER_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.PERSON A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'gender' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( GENDER_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(GENDER_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(GENDER_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3006' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'GENDER_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.PERSON A
    WHERE  GENDER_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(YEAR_OF_BIRTH AS VARCHAR),1,30) AS ERR_VALUE, 'ID3007' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'YEAR_OF_BIRTH' AS COL_NM 
FROM YourSchemaName.PERSON
WHERE CASE WHEN YEAR_OF_BIRTH NOT LIKE '[0-9][0-9][0-9][0-9]'
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (YEAR_OF_BIRTH IS NOT NULL AND DATALENGTH(CAST(YEAR_OF_BIRTH AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(YEAR_OF_BIRTH AS VARCHAR),1,30) AS ERR_VALUE, 'ID3008' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'YEAR_OF_BIRTH' AS COL_NM 
    FROM YourSchemaName.PERSON A
    WHERE  YEAR_OF_BIRTH IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(YEAR_OF_BIRTH AS VARCHAR),1,30) AS ERR_VALUE, 'ID3009' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'YEAR_OF_BIRTH' AS COL_NM 
FROM YourSchemaName.PERSON A
WHERE  YEAR_OF_BIRTH IS NOT NULL AND DATALENGTH(CAST(YEAR_OF_BIRTH AS VARCHAR)) > 0 
AND YEAR_OF_BIRTH NOT BETWEEN 1900 AND YEAR(GETDATE())
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(MONTH_OF_BIRTH AS VARCHAR),1,30) AS ERR_VALUE, 'ID3010' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'MONTH_OF_BIRTH' AS COL_NM 
FROM YourSchemaName.PERSON A
WHERE  MONTH_OF_BIRTH IS NOT NULL AND DATALENGTH(CAST(MONTH_OF_BIRTH AS VARCHAR)) > 0 
AND MONTH_OF_BIRTH NOT BETWEEN 1 AND 12

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DAY_OF_BIRTH AS VARCHAR),1,30) AS ERR_VALUE, 'ID3011' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'DAY_OF_BIRTH' AS COL_NM 
FROM YourSchemaName.PERSON A
WHERE  DAY_OF_BIRTH IS NOT NULL AND DATALENGTH(CAST(DAY_OF_BIRTH AS VARCHAR)) > 0 
AND DAY_OF_BIRTH NOT BETWEEN 1 AND 31

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(RACE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3013' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'RACE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.PERSON A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'race' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( RACE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(RACE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(RACE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3014' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'RACE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.PERSON A
    WHERE  RACE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(ETHNICITY_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3015' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'ETHNICITY_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.PERSON A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'ethnicity' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( ETHNICITY_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(ETHNICITY_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(ETHNICITY_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3016' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'ETHNICITY_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.PERSON A
    WHERE  ETHNICITY_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(LOCATION_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3017' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'LOCATION_ID' AS COL_NM 
FROM YourSchemaName.PERSON A
WHERE LOCATION_ID IS NOT NULL AND DATALENGTH(CAST(LOCATION_ID AS VARCHAR))>0
AND LOCATION_ID NOT BETWEEN 0 AND 99999

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;





BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(GENDER_SOURCE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3022' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'GENDER_SOURCE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.PERSON A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.GENDER_SOURCE_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( GENDER_SOURCE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(GENDER_SOURCE_CONCEPT_ID AS VARCHAR))>0 ) 
AND GENDER_SOURCE_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(RACE_SOURCE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3024' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'RACE_SOURCE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.PERSON A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.RACE_SOURCE_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( RACE_SOURCE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(RACE_SOURCE_CONCEPT_ID AS VARCHAR))>0 ) 
AND RACE_SOURCE_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;




BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_PERIOD_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3028' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_ID' AS COL_NM 
    FROM YourSchemaName.OBSERVATION_PERIOD A
    WHERE  OBSERVATION_PERIOD_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3029' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.OBSERVATION_PERIOD A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.PERSON B WHERE B.PERSON_ID = A.PERSON_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3030' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
    FROM YourSchemaName.OBSERVATION_PERIOD A
    WHERE  PERSON_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_PERIOD_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3031' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_START_DATE' AS COL_NM 
FROM YourSchemaName.OBSERVATION_PERIOD A
WHERE  OBSERVATION_PERIOD_START_DATE IS NOT NULL AND DATALENGTH(CAST(OBSERVATION_PERIOD_START_DATE AS VARCHAR)) > 0 
AND OBSERVATION_PERIOD_START_DATE > OBSERVATION_PERIOD_END_DATE

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_PERIOD_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3032' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_START_DATE' AS COL_NM 
    FROM YourSchemaName.OBSERVATION_PERIOD A
    WHERE  OBSERVATION_PERIOD_START_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_PERIOD_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3033' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_START_DATE' AS COL_NM 
FROM YourSchemaName.OBSERVATION_PERIOD A
WHERE  OBSERVATION_PERIOD_START_DATE IS NOT NULL AND DATALENGTH(CAST(OBSERVATION_PERIOD_START_DATE AS VARCHAR)) > 0 
AND OBSERVATION_PERIOD_START_DATE NOT BETWEEN '1950-01-01' AND CONVERT(varchar, GETDATE(), 23)

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_PERIOD_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3034' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_START_DATE' AS COL_NM 
FROM YourSchemaName.OBSERVATION_PERIOD A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.OBSERVATION_PERIOD_START_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( OBSERVATION_PERIOD_START_DATE IS NOT NULL AND DATALENGTH(CAST(OBSERVATION_PERIOD_START_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_PERIOD_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3035' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_END_DATE' AS COL_NM 
FROM YourSchemaName.OBSERVATION_PERIOD A
WHERE  OBSERVATION_PERIOD_END_DATE IS NOT NULL AND DATALENGTH(CAST(OBSERVATION_PERIOD_END_DATE AS VARCHAR)) > 0 
AND OBSERVATION_PERIOD_END_DATE < OBSERVATION_PERIOD_START_DATE

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_PERIOD_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3036' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_END_DATE' AS COL_NM 
    FROM YourSchemaName.OBSERVATION_PERIOD A
    WHERE  OBSERVATION_PERIOD_END_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_PERIOD_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3037' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_END_DATE' AS COL_NM 
FROM YourSchemaName.OBSERVATION_PERIOD A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.OBSERVATION_PERIOD_END_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( OBSERVATION_PERIOD_END_DATE IS NOT NULL AND DATALENGTH(CAST(OBSERVATION_PERIOD_END_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERIOD_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3038' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'PERIOD_TYPE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.OBSERVATION_PERIOD A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'type concept' and lower(concept_class_id) = 'type concept' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERIOD_TYPE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(PERIOD_TYPE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERIOD_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3039' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'PERIOD_TYPE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.OBSERVATION_PERIOD A
    WHERE  PERIOD_TYPE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_OCCURRENCE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3041' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM 
    FROM YourSchemaName.VISIT_OCCURRENCE A
    WHERE  VISIT_OCCURRENCE_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3042' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.VISIT_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.PERSON B WHERE B.PERSON_ID = A.PERSON_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3043' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
    FROM YourSchemaName.VISIT_OCCURRENCE A
    WHERE  PERSON_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3044' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.VISIT_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'visit' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3045' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.VISIT_OCCURRENCE A
    WHERE  VISIT_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3046' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATE' AS COL_NM 
FROM YourSchemaName.VISIT_OCCURRENCE A
WHERE  VISIT_START_DATE IS NOT NULL AND DATALENGTH(CAST(VISIT_START_DATE AS VARCHAR)) > 0 
AND VISIT_START_DATE > VISIT_END_DATE

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3047' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATE' AS COL_NM 
    FROM YourSchemaName.VISIT_OCCURRENCE A
    WHERE  VISIT_START_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3048' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATE' AS COL_NM 
FROM YourSchemaName.VISIT_OCCURRENCE A
WHERE  VISIT_START_DATE IS NOT NULL AND DATALENGTH(CAST(VISIT_START_DATE AS VARCHAR)) > 0 
AND VISIT_START_DATE NOT BETWEEN '1950-01-01' AND CONVERT(varchar, GETDATE(), 23)

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3049' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATE' AS COL_NM 
FROM YourSchemaName.VISIT_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.VISIT_START_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_START_DATE IS NOT NULL AND DATALENGTH(CAST(VISIT_START_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(VISIT_START_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3050' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATETIME' AS COL_NM 
FROM YourSchemaName.VISIT_OCCURRENCE
WHERE CASE WHEN CAST (VISIT_START_DATETIME AS DATE) <> VISIT_START_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (VISIT_START_DATETIME IS NOT NULL AND DATALENGTH(CAST(VISIT_START_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3051' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_END_DATE' AS COL_NM 
FROM YourSchemaName.VISIT_OCCURRENCE A
WHERE  VISIT_END_DATE IS NOT NULL AND DATALENGTH(CAST(VISIT_END_DATE AS VARCHAR)) > 0 
AND VISIT_END_DATE < VISIT_START_DATE

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3052' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_END_DATE' AS COL_NM 
    FROM YourSchemaName.VISIT_OCCURRENCE A
    WHERE  VISIT_END_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3053' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_END_DATE' AS COL_NM 
FROM YourSchemaName.VISIT_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.VISIT_END_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_END_DATE IS NOT NULL AND DATALENGTH(CAST(VISIT_END_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(VISIT_END_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3054' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_END_DATETIME' AS COL_NM 
FROM YourSchemaName.VISIT_OCCURRENCE
WHERE CASE WHEN CAST (VISIT_END_DATETIME AS DATE) <> VISIT_END_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (VISIT_END_DATETIME IS NOT NULL AND DATALENGTH(CAST(VISIT_END_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'IDINTEGER' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_TYPE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.VISIT_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'type concept' and lower(concept_class_id) = 'type concept' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_TYPE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_TYPE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3056' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_TYPE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.VISIT_OCCURRENCE A
    WHERE  VISIT_TYPE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;




BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_SOURCE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3060' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_SOURCE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.VISIT_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.VISIT_SOURCE_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_SOURCE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_SOURCE_CONCEPT_ID AS VARCHAR))>0 ) 
AND VISIT_SOURCE_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(ADMITTED_FROM_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3061' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'ADMITTED_FROM_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.VISIT_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.ADMITTED_FROM_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( ADMITTED_FROM_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(ADMITTED_FROM_CONCEPT_ID AS VARCHAR))>0 ) 
AND ADMITTED_FROM_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DISCHARGED_TO_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3063' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'DISCHARGED_TO_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.VISIT_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'visit' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DISCHARGED_TO_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(DISCHARGED_TO_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;




BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3067' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_ID' AS COL_NM 
    FROM YourSchemaName.VISIT_DETAIL A
    WHERE  VISIT_DETAIL_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3068' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.PERSON B WHERE B.PERSON_ID = A.PERSON_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3069' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
    FROM YourSchemaName.VISIT_DETAIL A
    WHERE  PERSON_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3070' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'visit' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_DETAIL_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_DETAIL_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3071' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.VISIT_DETAIL A
    WHERE  VISIT_DETAIL_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3072' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATE' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL A
WHERE  VISIT_DETAIL_START_DATE IS NOT NULL AND DATALENGTH(CAST(VISIT_DETAIL_START_DATE AS VARCHAR)) > 0 
AND VISIT_DETAIL_START_DATE > VISIT_DETAIL_END_DATE

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3073' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATE' AS COL_NM 
    FROM YourSchemaName.VISIT_DETAIL A
    WHERE  VISIT_DETAIL_START_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3074' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATE' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.VISIT_DETAIL_START_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_DETAIL_START_DATE IS NOT NULL AND DATALENGTH(CAST(VISIT_DETAIL_START_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3075' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATE' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID
AND A.VISIT_DETAIL_START_DATE BETWEEN DATEADD(DAY, -7, B.VISIT_START_DATE) 
                                  AND DATEADD(DAY, 7, B.VISIT_START_DATE)
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_DETAIL_START_DATE IS NOT NULL AND DATALENGTH(CAST(VISIT_DETAIL_START_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_START_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3076' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATETIME' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL
WHERE CASE WHEN CAST (VISIT_DETAIL_START_DATETIME AS DATE) <> VISIT_DETAIL_START_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (VISIT_DETAIL_START_DATETIME IS NOT NULL AND DATALENGTH(CAST(VISIT_DETAIL_START_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'IDDATE' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATE' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL A
WHERE  VISIT_DETAIL_END_DATE IS NOT NULL AND DATALENGTH(CAST(VISIT_DETAIL_END_DATE AS VARCHAR)) > 0 
AND VISIT_DETAIL_END_DATE < VISIT_DETAIL_START_DATE

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3078' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATE' AS COL_NM 
    FROM YourSchemaName.VISIT_DETAIL A
    WHERE  VISIT_DETAIL_END_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3079' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATE' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.VISIT_DETAIL_END_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_DETAIL_END_DATE IS NOT NULL AND DATALENGTH(CAST(VISIT_DETAIL_END_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3080' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATE' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID
AND A.VISIT_DETAIL_END_DATE BETWEEN DATEADD(DAY, -7, B.VISIT_END_DATE) 
                                  AND DATEADD(DAY, 7, B.VISIT_END_DATE)
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_DETAIL_END_DATE IS NOT NULL AND DATALENGTH(CAST(VISIT_DETAIL_END_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_END_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3081' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATETIME' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL
WHERE CASE WHEN CAST (VISIT_DETAIL_END_DATETIME AS DATE) <> VISIT_DETAIL_END_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (VISIT_DETAIL_END_DATETIME IS NOT NULL AND DATALENGTH(CAST(VISIT_DETAIL_END_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3082' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_TYPE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'type concept' and lower(concept_class_id) = 'type concept' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_DETAIL_TYPE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_DETAIL_TYPE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3083' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_TYPE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.VISIT_DETAIL A
    WHERE  VISIT_DETAIL_TYPE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;




BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_DETAIL_SOURCE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3087' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_SOURCE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.VISIT_DETAIL_SOURCE_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_DETAIL_SOURCE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_DETAIL_SOURCE_CONCEPT_ID AS VARCHAR))>0 ) 
AND VISIT_DETAIL_SOURCE_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(ADMITTED_FROM_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3089' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'ADMITTED_FROM_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.ADMITTED_FROM_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( ADMITTED_FROM_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(ADMITTED_FROM_CONCEPT_ID AS VARCHAR))>0 ) 
AND ADMITTED_FROM_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DISCHARGED_TO_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3091' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'DISCHARGED_TO_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'visit' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DISCHARGED_TO_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(DISCHARGED_TO_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;



BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_OCCURRENCE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3094' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM 
FROM YourSchemaName.VISIT_DETAIL A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_OCCURRENCE_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_OCCURRENCE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3095' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM 
    FROM YourSchemaName.VISIT_DETAIL A
    WHERE  VISIT_OCCURRENCE_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(CONDITION_OCCURRENCE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3097' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_OCCURRENCE_ID' AS COL_NM 
    FROM YourSchemaName.CONDITION_OCCURRENCE A
    WHERE  CONDITION_OCCURRENCE_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3098' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.CONDITION_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.PERSON B WHERE B.PERSON_ID = A.PERSON_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3099' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
    FROM YourSchemaName.CONDITION_OCCURRENCE A
    WHERE  PERSON_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(CONDITION_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3100' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.CONDITION_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'condition' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( CONDITION_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(CONDITION_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(CONDITION_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3101' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.CONDITION_OCCURRENCE A
    WHERE  CONDITION_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(CONDITION_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3102' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.CONDITION_OCCURRENCE A LEFT JOIN YourSchemaName.PERSON B ON A.PERSON_ID = B.PERSON_ID
WHERE  B.GENDER_CONCEPT_ID != 8507  
AND ( CONDITION_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(CONDITION_CONCEPT_ID AS VARCHAR))>0 ) 
AND CONDITION_CONCEPT_ID IN (
SELECT DESCENDANT_CONCEPT_ID FROM YourSchemaName.CONCEPT_ANCESTOR WHERE ANCESTOR_CONCEPT_ID IN (45771080,4216771,3655355,4090861,4057948)
)
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(CONDITION_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3103' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.CONDITION_OCCURRENCE A LEFT JOIN YourSchemaName.PERSON B ON A.PERSON_ID = B.PERSON_ID
WHERE  B.GENDER_CONCEPT_ID != 8532  
AND ( CONDITION_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(CONDITION_CONCEPT_ID AS VARCHAR))>0 ) 
AND CONDITION_CONCEPT_ID IN (
SELECT DESCENDANT_CONCEPT_ID FROM YourSchemaName.CONCEPT_ANCESTOR WHERE ANCESTOR_CONCEPT_ID IN (4129486,4095793,443343,4268895,4056331)
)
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(CONDITION_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3104' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM 
FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CASE WHEN CONDITION_START_DATE NOT LIKE '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]'
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (CONDITION_START_DATE IS NOT NULL AND DATALENGTH(CAST(CONDITION_START_DATE AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(CONDITION_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3105' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM 
    FROM YourSchemaName.CONDITION_OCCURRENCE A
    WHERE  CONDITION_START_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(CONDITION_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3106' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM 
FROM YourSchemaName.CONDITION_OCCURRENCE A
WHERE  CONDITION_START_DATE IS NOT NULL AND DATALENGTH(CAST(CONDITION_START_DATE AS VARCHAR)) > 0 
AND CONDITION_START_DATE NOT BETWEEN '1950-01-01' AND CONVERT(varchar, GETDATE(), 23)

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(CONDITION_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3107' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM 
FROM YourSchemaName.CONDITION_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.CONDITION_START_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( CONDITION_START_DATE IS NOT NULL AND DATALENGTH(CAST(CONDITION_START_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(CONDITION_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3108' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM 
FROM YourSchemaName.CONDITION_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID
AND A.CONDITION_START_DATE BETWEEN DATEADD(DAY, -7, B.VISIT_START_DATE) 
                                  AND DATEADD(DAY, 7, B.VISIT_START_DATE)
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( CONDITION_START_DATE IS NOT NULL AND DATALENGTH(CAST(CONDITION_START_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(CONDITION_START_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3109' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATETIME' AS COL_NM 
FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CASE WHEN CAST (CONDITION_START_DATETIME AS DATE) <> CONDITION_START_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (CONDITION_START_DATETIME IS NOT NULL AND DATALENGTH(CAST(CONDITION_START_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;



BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(CONDITION_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3112' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_TYPE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.CONDITION_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'type concept' and lower(concept_class_id) = 'type concept' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( CONDITION_TYPE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(CONDITION_TYPE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(CONDITION_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3113' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_TYPE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.CONDITION_OCCURRENCE A
    WHERE  CONDITION_TYPE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(CONDITION_STATUS_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3114' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_STATUS_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.CONDITION_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'condition status' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( CONDITION_STATUS_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(CONDITION_STATUS_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;



BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_OCCURRENCE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3117' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM 
FROM YourSchemaName.CONDITION_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_OCCURRENCE_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;






BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_EXPOSURE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3123' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_ID' AS COL_NM 
    FROM YourSchemaName.DRUG_EXPOSURE A
    WHERE  DRUG_EXPOSURE_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3124' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.PERSON B WHERE B.PERSON_ID = A.PERSON_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3125' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
    FROM YourSchemaName.DRUG_EXPOSURE A
    WHERE  PERSON_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3126' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'drug' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DRUG_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(DRUG_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3127' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.DRUG_EXPOSURE A
    WHERE  DRUG_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_EXPOSURE_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3128' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE  DRUG_EXPOSURE_START_DATE IS NOT NULL AND DATALENGTH(CAST(DRUG_EXPOSURE_START_DATE AS VARCHAR)) > 0 
AND DRUG_EXPOSURE_START_DATE > DRUG_EXPOSURE_END_DATE

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_EXPOSURE_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3129' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM 
    FROM YourSchemaName.DRUG_EXPOSURE A
    WHERE  DRUG_EXPOSURE_START_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_EXPOSURE_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3130' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE  DRUG_EXPOSURE_START_DATE IS NOT NULL AND DATALENGTH(CAST(DRUG_EXPOSURE_START_DATE AS VARCHAR)) > 0 
AND DRUG_EXPOSURE_START_DATE NOT BETWEEN '1950-01-01' AND CONVERT(varchar, GETDATE(), 23)

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_EXPOSURE_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3131' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.DRUG_EXPOSURE_START_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DRUG_EXPOSURE_START_DATE IS NOT NULL AND DATALENGTH(CAST(DRUG_EXPOSURE_START_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_EXPOSURE_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3132' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID
AND A.DRUG_EXPOSURE_START_DATE BETWEEN DATEADD(DAY, -7, B.VISIT_START_DATE) 
                                  AND DATEADD(DAY, 7, B.VISIT_START_DATE)
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DRUG_EXPOSURE_START_DATE IS NOT NULL AND DATALENGTH(CAST(DRUG_EXPOSURE_START_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(DRUG_EXPOSURE_START_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3133' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATETIME' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE
WHERE CASE WHEN CAST (DRUG_EXPOSURE_START_DATETIME AS DATE) <> DRUG_EXPOSURE_START_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (DRUG_EXPOSURE_START_DATETIME IS NOT NULL AND DATALENGTH(CAST(DRUG_EXPOSURE_START_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_EXPOSURE_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3134' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_END_DATE' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE  DRUG_EXPOSURE_END_DATE IS NOT NULL AND DATALENGTH(CAST(DRUG_EXPOSURE_END_DATE AS VARCHAR)) > 0 
AND DRUG_EXPOSURE_END_DATE < DRUG_EXPOSURE_START_DATE

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_EXPOSURE_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3135' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_END_DATE' AS COL_NM 
    FROM YourSchemaName.DRUG_EXPOSURE A
    WHERE  DRUG_EXPOSURE_END_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_EXPOSURE_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3136' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_END_DATE' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.DRUG_EXPOSURE_END_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DRUG_EXPOSURE_END_DATE IS NOT NULL AND DATALENGTH(CAST(DRUG_EXPOSURE_END_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;



BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3139' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_TYPE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'type concept' and lower(concept_class_id) = 'type concept' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DRUG_TYPE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(DRUG_TYPE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3140' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_TYPE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.DRUG_EXPOSURE A
    WHERE  DRUG_TYPE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;



BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(QUANTITY AS VARCHAR),1,30) AS ERR_VALUE, 'ID3143' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'QUANTITY' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE  QUANTITY IS NOT NULL AND DATALENGTH(CAST(QUANTITY AS VARCHAR)) > 0 
AND TRY_CAST(QUANTITY AS FLOAT) IS NULL

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DAYS_SUPPLY AS VARCHAR),1,30) AS ERR_VALUE, 'ID3144' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DAYS_SUPPLY' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE  DAYS_SUPPLY IS NOT NULL AND DATALENGTH(CAST(DAYS_SUPPLY AS VARCHAR)) > 0 
AND TRY_CAST(DAYS_SUPPLY AS FLOAT) IS NULL

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(ROUTE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3146' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'ROUTE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'route' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( ROUTE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(ROUTE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;



BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_OCCURRENCE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3149' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_OCCURRENCE_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;



BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DRUG_SOURCE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3152' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_SOURCE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.DRUG_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.DRUG_SOURCE_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DRUG_SOURCE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(DRUG_SOURCE_CONCEPT_ID AS VARCHAR))>0 ) 
AND DRUG_SOURCE_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;




BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_OCCURRENCE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3156' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_OCCURRENCE_ID' AS COL_NM 
    FROM YourSchemaName.PROCEDURE_OCCURRENCE A
    WHERE  PROCEDURE_OCCURRENCE_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3157' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.PERSON B WHERE B.PERSON_ID = A.PERSON_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3158' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
    FROM YourSchemaName.PROCEDURE_OCCURRENCE A
    WHERE  PERSON_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3159' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'procedure' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PROCEDURE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(PROCEDURE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3160' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.PROCEDURE_OCCURRENCE A
    WHERE  PROCEDURE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3161' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE A
WHERE  PROCEDURE_DATE IS NOT NULL AND DATALENGTH(CAST(PROCEDURE_DATE AS VARCHAR)) > 0 
AND PROCEDURE_DATE > PROCEDURE_END_DATE

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3162' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM 
    FROM YourSchemaName.PROCEDURE_OCCURRENCE A
    WHERE  PROCEDURE_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3163' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE A
WHERE  PROCEDURE_DATE IS NOT NULL AND DATALENGTH(CAST(PROCEDURE_DATE AS VARCHAR)) > 0 
AND PROCEDURE_DATE NOT BETWEEN '1950-01-01' AND CONVERT(varchar, GETDATE(), 23)

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3164' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.PROCEDURE_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PROCEDURE_DATE IS NOT NULL AND DATALENGTH(CAST(PROCEDURE_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3165' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID
AND A.PROCEDURE_DATE BETWEEN DATEADD(DAY, -7, B.VISIT_START_DATE) 
                                  AND DATEADD(DAY, 7, B.VISIT_START_DATE)
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PROCEDURE_DATE IS NOT NULL AND DATALENGTH(CAST(PROCEDURE_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3166' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATETIME' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE CASE WHEN CAST (PROCEDURE_DATETIME AS DATE) <> PROCEDURE_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (PROCEDURE_DATETIME IS NOT NULL AND DATALENGTH(CAST(PROCEDURE_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3167' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_END_DATE' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE A
WHERE  PROCEDURE_END_DATE IS NOT NULL AND DATALENGTH(CAST(PROCEDURE_END_DATE AS VARCHAR)) > 0 
AND PROCEDURE_END_DATE < PROCEDURE_DATE

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3168' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_END_DATE' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.PROCEDURE_END_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PROCEDURE_END_DATE IS NOT NULL AND DATALENGTH(CAST(PROCEDURE_END_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_END_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3169' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_END_DATETIME' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE CASE WHEN CAST (PROCEDURE_END_DATETIME AS DATE) <> PROCEDURE_END_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (PROCEDURE_END_DATETIME IS NOT NULL AND DATALENGTH(CAST(PROCEDURE_END_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3170' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_TYPE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'type concept' and lower(concept_class_id) = 'type concept' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PROCEDURE_TYPE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(PROCEDURE_TYPE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PROCEDURE_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3171' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_TYPE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.PROCEDURE_OCCURRENCE A
    WHERE  PROCEDURE_TYPE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(MODIFIER_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3172' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'MODIFIER_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(vocabulary_id) in ('cpt4', 'hcpcs') and lower(concept_class_id) in ('cpt4 modifier', 'hcpcs modifier') and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( MODIFIER_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(MODIFIER_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(QUANTITY AS VARCHAR),1,30) AS ERR_VALUE, 'ID3173' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'QUANTITY' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE A
WHERE  QUANTITY IS NOT NULL AND DATALENGTH(CAST(QUANTITY AS VARCHAR)) > 0 
AND TRY_CAST(QUANTITY AS FLOAT) IS NULL

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_OCCURRENCE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3175' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM 
FROM YourSchemaName.PROCEDURE_OCCURRENCE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_OCCURRENCE_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;






BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEVICE_EXPOSURE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3181' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_ID' AS COL_NM 
    FROM YourSchemaName.DEVICE_EXPOSURE A
    WHERE  DEVICE_EXPOSURE_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3182' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.PERSON B WHERE B.PERSON_ID = A.PERSON_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3183' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
    FROM YourSchemaName.DEVICE_EXPOSURE A
    WHERE  PERSON_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEVICE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3184' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'device' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DEVICE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(DEVICE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEVICE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3185' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.DEVICE_EXPOSURE A
    WHERE  DEVICE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEVICE_EXPOSURE_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3186' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATE' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE A
WHERE  DEVICE_EXPOSURE_START_DATE IS NOT NULL AND DATALENGTH(CAST(DEVICE_EXPOSURE_START_DATE AS VARCHAR)) > 0 
AND DEVICE_EXPOSURE_START_DATE > DEVICE_EXPOSURE_END_DATE

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEVICE_EXPOSURE_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3187' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATE' AS COL_NM 
    FROM YourSchemaName.DEVICE_EXPOSURE A
    WHERE  DEVICE_EXPOSURE_START_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEVICE_EXPOSURE_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3188' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATE' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.DEVICE_EXPOSURE_START_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DEVICE_EXPOSURE_START_DATE IS NOT NULL AND DATALENGTH(CAST(DEVICE_EXPOSURE_START_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEVICE_EXPOSURE_START_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3189' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATE' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID
AND A.DEVICE_EXPOSURE_START_DATE BETWEEN DATEADD(DAY, -7, B.VISIT_START_DATE) 
                                  AND DATEADD(DAY, 7, B.VISIT_START_DATE)
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DEVICE_EXPOSURE_START_DATE IS NOT NULL AND DATALENGTH(CAST(DEVICE_EXPOSURE_START_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(DEVICE_EXPOSURE_START_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3190' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATETIME' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE
WHERE CASE WHEN CAST (DEVICE_EXPOSURE_START_DATETIME AS DATE) <> DEVICE_EXPOSURE_START_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (DEVICE_EXPOSURE_START_DATETIME IS NOT NULL AND DATALENGTH(CAST(DEVICE_EXPOSURE_START_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEVICE_EXPOSURE_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3191' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_END_DATE' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE A
WHERE  DEVICE_EXPOSURE_END_DATE IS NOT NULL AND DATALENGTH(CAST(DEVICE_EXPOSURE_END_DATE AS VARCHAR)) > 0 
AND DEVICE_EXPOSURE_END_DATE < DEVICE_EXPOSURE_START_DATE

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEVICE_EXPOSURE_END_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3192' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_END_DATE' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.DEVICE_EXPOSURE_END_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DEVICE_EXPOSURE_END_DATE IS NOT NULL AND DATALENGTH(CAST(DEVICE_EXPOSURE_END_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(DEVICE_EXPOSURE_END_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3193' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_END_DATETIME' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE
WHERE CASE WHEN CAST (DEVICE_EXPOSURE_END_DATETIME AS DATE) <> DEVICE_EXPOSURE_END_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (DEVICE_EXPOSURE_END_DATETIME IS NOT NULL AND DATALENGTH(CAST(DEVICE_EXPOSURE_END_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEVICE_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3194' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_TYPE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'type concept' and lower(concept_class_id) = 'type concept' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DEVICE_TYPE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(DEVICE_TYPE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEVICE_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3195' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_TYPE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.DEVICE_EXPOSURE A
    WHERE  DEVICE_TYPE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;



BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(QUANTITY AS VARCHAR),1,30) AS ERR_VALUE, 'ID3198' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'QUANTITY' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE A
WHERE  QUANTITY IS NOT NULL AND DATALENGTH(CAST(QUANTITY AS VARCHAR)) > 0 
AND TRY_CAST(QUANTITY AS FLOAT) IS NULL

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_OCCURRENCE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3200' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_OCCURRENCE_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;



BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEVICE_SOURCE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3203' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_SOURCE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.DEVICE_SOURCE_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DEVICE_SOURCE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(DEVICE_SOURCE_CONCEPT_ID AS VARCHAR))>0 ) 
AND DEVICE_SOURCE_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(UNIT_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3204' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'UNIT_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'unit' and lower(standard_concept) = 'standard')  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( UNIT_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(UNIT_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(UNIT_SOURCE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3206' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'UNIT_SOURCE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.DEVICE_EXPOSURE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.UNIT_SOURCE_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( UNIT_SOURCE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(UNIT_SOURCE_CONCEPT_ID AS VARCHAR))>0 ) 
AND UNIT_SOURCE_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(MEASUREMENT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3208' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_ID' AS COL_NM 
    FROM YourSchemaName.MEASUREMENT A
    WHERE  MEASUREMENT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3209' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.PERSON B WHERE B.PERSON_ID = A.PERSON_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3210' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
    FROM YourSchemaName.MEASUREMENT A
    WHERE  PERSON_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(MEASUREMENT_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3211' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'measurement' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( MEASUREMENT_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(MEASUREMENT_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(MEASUREMENT_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3212' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.MEASUREMENT A
    WHERE  MEASUREMENT_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(MEASUREMENT_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3213' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM 
FROM YourSchemaName.MEASUREMENT
WHERE CASE WHEN MEASUREMENT_DATE NOT LIKE '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]'
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (MEASUREMENT_DATE IS NOT NULL AND DATALENGTH(CAST(MEASUREMENT_DATE AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(MEASUREMENT_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3214' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM 
    FROM YourSchemaName.MEASUREMENT A
    WHERE  MEASUREMENT_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(MEASUREMENT_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3215' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE  MEASUREMENT_DATE IS NOT NULL AND DATALENGTH(CAST(MEASUREMENT_DATE AS VARCHAR)) > 0 
AND MEASUREMENT_DATE NOT BETWEEN '1950-01-01' AND CONVERT(varchar, GETDATE(), 23)

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(MEASUREMENT_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3216' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.MEASUREMENT_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( MEASUREMENT_DATE IS NOT NULL AND DATALENGTH(CAST(MEASUREMENT_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(MEASUREMENT_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3217' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID
AND A.MEASUREMENT_DATE BETWEEN DATEADD(DAY, -7, B.VISIT_START_DATE) 
                                  AND DATEADD(DAY, 7, B.VISIT_START_DATE)
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( MEASUREMENT_DATE IS NOT NULL AND DATALENGTH(CAST(MEASUREMENT_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(MEASUREMENT_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3218' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATETIME' AS COL_NM 
FROM YourSchemaName.MEASUREMENT
WHERE CASE WHEN CAST (MEASUREMENT_DATETIME AS DATE) <> MEASUREMENT_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (MEASUREMENT_DATETIME IS NOT NULL AND DATALENGTH(CAST(MEASUREMENT_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(MEASUREMENT_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3220' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_TYPE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'type concept' and lower(concept_class_id) = 'type concept' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( MEASUREMENT_TYPE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(MEASUREMENT_TYPE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(MEASUREMENT_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3221' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_TYPE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.MEASUREMENT A
    WHERE  MEASUREMENT_TYPE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OPERATOR_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3222' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'OPERATOR_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'meas value operator' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( OPERATOR_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(OPERATOR_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VALUE_AS_NUMBER AS VARCHAR),1,30) AS ERR_VALUE, 'ID3223' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'VALUE_AS_NUMBER' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE  VALUE_AS_NUMBER IS NOT NULL AND DATALENGTH(CAST(VALUE_AS_NUMBER AS VARCHAR)) > 0 
AND TRY_CAST(VALUE_AS_NUMBER AS FLOAT) IS NULL

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VALUE_AS_NUMBER AS VARCHAR),1,30) AS ERR_VALUE, 'ID3224' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'VALUE_AS_NUMBER' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE VALUE_AS_NUMBER IS NULL
AND UNIT_CONCEPT_ID IS NOT NULL
AND UNIT_CONCEPT_ID != 0 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VALUE_AS_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3225' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'VALUE_AS_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.VALUE_AS_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VALUE_AS_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(VALUE_AS_CONCEPT_ID AS VARCHAR))>0 ) 
AND VALUE_AS_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(UNIT_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3226' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'UNIT_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.UNIT_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( UNIT_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(UNIT_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(RANGE_LOW AS VARCHAR),1,30) AS ERR_VALUE, 'ID3227' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'RANGE_LOW' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE  RANGE_LOW IS NOT NULL AND DATALENGTH(CAST(RANGE_LOW AS VARCHAR)) > 0 
AND TRY_CAST(RANGE_LOW AS FLOAT) IS NULL

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(RANGE_HIGH AS VARCHAR),1,30) AS ERR_VALUE, 'ID3228' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'RANGE_HIGH' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE  RANGE_HIGH IS NOT NULL AND DATALENGTH(CAST(RANGE_HIGH AS VARCHAR)) > 0 
AND TRY_CAST(RANGE_HIGH AS FLOAT) IS NULL

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_OCCURRENCE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3230' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_OCCURRENCE_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;





BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(UNIT_SOURCE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3235' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'UNIT_SOURCE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.MEASUREMENT A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.UNIT_SOURCE_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( UNIT_SOURCE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(UNIT_SOURCE_CONCEPT_ID AS VARCHAR))>0 ) 
AND UNIT_SOURCE_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;





BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3240' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_ID' AS COL_NM 
    FROM YourSchemaName.OBSERVATION A
    WHERE  OBSERVATION_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3241' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.OBSERVATION A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.PERSON B WHERE B.PERSON_ID = A.PERSON_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3242' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
    FROM YourSchemaName.OBSERVATION A
    WHERE  PERSON_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3243' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.OBSERVATION A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.OBSERVATION_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( OBSERVATION_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(OBSERVATION_CONCEPT_ID AS VARCHAR))>0 ) 
AND OBSERVATION_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3244' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.OBSERVATION A
    WHERE  OBSERVATION_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3245' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM 
FROM YourSchemaName.OBSERVATION
WHERE CASE WHEN OBSERVATION_DATE NOT LIKE '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]'
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (OBSERVATION_DATE IS NOT NULL AND DATALENGTH(CAST(OBSERVATION_DATE AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3246' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM 
    FROM YourSchemaName.OBSERVATION A
    WHERE  OBSERVATION_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3247' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM 
FROM YourSchemaName.OBSERVATION A
WHERE  OBSERVATION_DATE IS NOT NULL AND DATALENGTH(CAST(OBSERVATION_DATE AS VARCHAR)) > 0 
AND OBSERVATION_DATE NOT BETWEEN '1950-01-01' AND CONVERT(varchar, GETDATE(), 23)

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3248' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM 
FROM YourSchemaName.OBSERVATION A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.OBSERVATION_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( OBSERVATION_DATE IS NOT NULL AND DATALENGTH(CAST(OBSERVATION_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3249' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM 
FROM YourSchemaName.OBSERVATION A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID
AND A.OBSERVATION_DATE BETWEEN DATEADD(DAY, -7, B.VISIT_START_DATE) 
                                  AND DATEADD(DAY, 7, B.VISIT_START_DATE)
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( OBSERVATION_DATE IS NOT NULL AND DATALENGTH(CAST(OBSERVATION_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3250' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATETIME' AS COL_NM 
FROM YourSchemaName.OBSERVATION
WHERE CASE WHEN CAST (OBSERVATION_DATETIME AS DATE) <> OBSERVATION_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (OBSERVATION_DATETIME IS NOT NULL AND DATALENGTH(CAST(OBSERVATION_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3251' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_TYPE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.OBSERVATION A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'type concept' and lower(concept_class_id) = 'type concept' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( OBSERVATION_TYPE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(OBSERVATION_TYPE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3252' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_TYPE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.OBSERVATION A
    WHERE  OBSERVATION_TYPE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VALUE_AS_NUMBER AS VARCHAR),1,30) AS ERR_VALUE, 'ID3253' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'VALUE_AS_NUMBER' AS COL_NM 
FROM YourSchemaName.OBSERVATION A
WHERE  VALUE_AS_NUMBER IS NOT NULL AND DATALENGTH(CAST(VALUE_AS_NUMBER AS VARCHAR)) > 0 
AND TRY_CAST(VALUE_AS_NUMBER AS FLOAT) IS NULL

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VALUE_AS_NUMBER AS VARCHAR),1,30) AS ERR_VALUE, 'ID3254' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'VALUE_AS_NUMBER' AS COL_NM 
FROM YourSchemaName.OBSERVATION A
WHERE VALUE_AS_NUMBER IS NULL
AND UNIT_CONCEPT_ID IS NOT NULL
AND UNIT_CONCEPT_ID != 0 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VALUE_AS_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3256' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'VALUE_AS_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.OBSERVATION A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.VALUE_AS_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VALUE_AS_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(VALUE_AS_CONCEPT_ID AS VARCHAR))>0 ) 
AND VALUE_AS_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(QUALIFIER_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3257' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'QUALIFIER_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.OBSERVATION A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.QUALIFIER_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( QUALIFIER_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(QUALIFIER_CONCEPT_ID AS VARCHAR))>0 ) 
AND QUALIFIER_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(UNIT_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3258' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'UNIT_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.OBSERVATION A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'unit' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( UNIT_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(UNIT_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_OCCURRENCE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3260' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM 
FROM YourSchemaName.OBSERVATION A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_OCCURRENCE_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;



BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(OBSERVATION_SOURCE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3263' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_SOURCE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.OBSERVATION A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.OBSERVATION_SOURCE_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( OBSERVATION_SOURCE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(OBSERVATION_SOURCE_CONCEPT_ID AS VARCHAR))>0 ) 
AND OBSERVATION_SOURCE_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;






BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3269' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM (
   SELECT
      PERSON_ID,
      COUNT(*) OVER (PARTITION BY PERSON_ID) AS CNT
    FROM YourSchemaName.DEATH A
    WHERE  PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR)) > 0 
         ) subquery
WHERE CNT > 1

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3270' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
    FROM YourSchemaName.DEATH A
    WHERE  PERSON_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3271' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.DEATH A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.PERSON B WHERE B.PERSON_ID = A.PERSON_ID
AND (CAST(B.YEAR_OF_BIRTH AS VARCHAR) + '-' 
      + RIGHT('00' + CAST(B.MONTH_OF_BIRTH AS VARCHAR), 2) + '-' 
      + RIGHT('00' + CAST(B.DAY_OF_BIRTH AS VARCHAR), 2)) < A.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(DEATH_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3272' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'DEATH_DATE' AS COL_NM 
FROM YourSchemaName.DEATH
WHERE CASE WHEN DEATH_DATE NOT LIKE '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]'
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (DEATH_DATE IS NOT NULL AND DATALENGTH(CAST(DEATH_DATE AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEATH_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3273' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'DEATH_DATE' AS COL_NM 
    FROM YourSchemaName.DEATH A
    WHERE  DEATH_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DEATH_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3275' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'DEATH_TYPE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.DEATH A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'type concept' and lower(concept_class_id) = 'type concept' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DEATH_TYPE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(DEATH_TYPE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(CAUSE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3276' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'CAUSE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.DEATH A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.CAUSE_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( CAUSE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(CAUSE_CONCEPT_ID AS VARCHAR))>0 ) 
AND CAUSE_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;




BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3280' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_ID' AS COL_NM 
    FROM YourSchemaName.NOTE A
    WHERE  NOTE_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3281' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.NOTE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.PERSON B WHERE B.PERSON_ID = A.PERSON_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3282' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
    FROM YourSchemaName.NOTE A
    WHERE  PERSON_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(NOTE_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3283' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATE' AS COL_NM 
FROM YourSchemaName.NOTE
WHERE CASE WHEN NOTE_DATE NOT LIKE '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]'
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (NOTE_DATE IS NOT NULL AND DATALENGTH(CAST(NOTE_DATE AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3284' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATE' AS COL_NM 
    FROM YourSchemaName.NOTE A
    WHERE  NOTE_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3285' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATE' AS COL_NM 
FROM YourSchemaName.NOTE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.NOTE_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( NOTE_DATE IS NOT NULL AND DATALENGTH(CAST(NOTE_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3286' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATE' AS COL_NM 
FROM YourSchemaName.NOTE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID
AND A.NOTE_DATE BETWEEN DATEADD(DAY, -7, B.VISIT_START_DATE) 
                                  AND DATEADD(DAY, 7, B.VISIT_START_DATE)
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( NOTE_DATE IS NOT NULL AND DATALENGTH(CAST(NOTE_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(NOTE_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3287' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATETIME' AS COL_NM 
FROM YourSchemaName.NOTE
WHERE CASE WHEN CAST (NOTE_DATETIME AS DATE) <> NOTE_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (NOTE_DATETIME IS NOT NULL AND DATALENGTH(CAST(NOTE_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3288' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_TYPE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.NOTE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'type concept' and lower(concept_class_id) = 'type concept' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( NOTE_TYPE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(NOTE_TYPE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3289' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_TYPE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.NOTE A
    WHERE  NOTE_TYPE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_CLASS_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3290' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_CLASS_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.NOTE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'meas value' and lower(concept_class_id) in ('doc kind', 'doc role', 'doc setting', 'doc subject matter', 'doc type of service'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( NOTE_CLASS_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(NOTE_CLASS_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_CLASS_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3291' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_CLASS_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.NOTE A
    WHERE  NOTE_CLASS_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;



BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_TEXT AS VARCHAR),1,30) AS ERR_VALUE, 'ID3294' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_TEXT' AS COL_NM 
    FROM YourSchemaName.NOTE A
    WHERE  NOTE_TEXT IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(ENCODING_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3295' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'ENCODING_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.NOTE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and concept_id = 32678)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( ENCODING_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(ENCODING_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(ENCODING_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3296' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'ENCODING_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.NOTE A
    WHERE  ENCODING_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(LANGUAGE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3297' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'LANGUAGE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.NOTE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and concept_id = 4182347)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( LANGUAGE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(LANGUAGE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(LANGUAGE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3298' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'LANGUAGE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.NOTE A
    WHERE  LANGUAGE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(VISIT_OCCURRENCE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3300' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM 
FROM YourSchemaName.NOTE A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.VISIT_OCCURRENCE B WHERE B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( VISIT_OCCURRENCE_ID IS NOT NULL AND DATALENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;






BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_NLP_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3306' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_NLP_ID' AS COL_NM 
    FROM YourSchemaName.NOTE_NLP A
    WHERE  NOTE_NLP_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3307' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_ID' AS COL_NM 
FROM YourSchemaName.NOTE_NLP A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.NOTE B WHERE B.NOTE_ID = A.NOTE_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( NOTE_ID IS NOT NULL AND DATALENGTH(CAST(NOTE_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3308' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_ID' AS COL_NM 
    FROM YourSchemaName.NOTE_NLP A
    WHERE  NOTE_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(SECTION_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3309' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'SECTION_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.NOTE_NLP A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.SECTION_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( SECTION_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(SECTION_CONCEPT_ID AS VARCHAR))>0 ) 
AND SECTION_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;




BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(LEXICAL_VARIANT AS VARCHAR),1,30) AS ERR_VALUE, 'ID3313' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'LEXICAL_VARIANT' AS COL_NM 
    FROM YourSchemaName.NOTE_NLP A
    WHERE  LEXICAL_VARIANT IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_NLP_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3314' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_NLP_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.NOTE_NLP A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.NOTE_NLP_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( NOTE_NLP_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(NOTE_NLP_CONCEPT_ID AS VARCHAR))>0 ) 
AND NOTE_NLP_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NOTE_NLP_SOURCE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3315' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_NLP_SOURCE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.NOTE_NLP A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.NOTE_NLP_SOURCE_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( NOTE_NLP_SOURCE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(NOTE_NLP_SOURCE_CONCEPT_ID AS VARCHAR))>0 ) 
AND NOTE_NLP_SOURCE_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;


BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(NLP_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3317' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NLP_DATE' AS COL_NM 
FROM YourSchemaName.NOTE_NLP
WHERE CASE WHEN NLP_DATE NOT LIKE '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]'
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (NLP_DATE IS NOT NULL AND DATALENGTH(CAST(NLP_DATE AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(NLP_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3318' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NLP_DATE' AS COL_NM 
    FROM YourSchemaName.NOTE_NLP A
    WHERE  NLP_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(NLP_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3319' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NLP_DATETIME' AS COL_NM 
FROM YourSchemaName.NOTE_NLP
WHERE CASE WHEN CAST (NLP_DATETIME AS DATE) <> NLP_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (NLP_DATETIME IS NOT NULL AND DATALENGTH(CAST(NLP_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;





BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(SPECIMEN_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3324' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_ID' AS COL_NM 
    FROM YourSchemaName.SPECIMEN A
    WHERE  SPECIMEN_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3325' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
FROM YourSchemaName.SPECIMEN A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.PERSON B WHERE B.PERSON_ID = A.PERSON_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( PERSON_ID IS NOT NULL AND DATALENGTH(CAST(PERSON_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(PERSON_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3326' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'PERSON_ID' AS COL_NM 
    FROM YourSchemaName.SPECIMEN A
    WHERE  PERSON_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(SPECIMEN_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3327' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.SPECIMEN A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'specimen' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( SPECIMEN_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(SPECIMEN_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(SPECIMEN_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3328' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.SPECIMEN A
    WHERE  SPECIMEN_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(SPECIMEN_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3329' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_TYPE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.SPECIMEN A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'type concept' and lower(concept_class_id) = 'type concept' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( SPECIMEN_TYPE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(SPECIMEN_TYPE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(SPECIMEN_TYPE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3330' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_TYPE_CONCEPT_ID' AS COL_NM 
    FROM YourSchemaName.SPECIMEN A
    WHERE  SPECIMEN_TYPE_CONCEPT_ID IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(SPECIMEN_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3331' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_DATE' AS COL_NM 
FROM YourSchemaName.SPECIMEN
WHERE CASE WHEN SPECIMEN_DATE NOT LIKE '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]'
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (SPECIMEN_DATE IS NOT NULL AND DATALENGTH(CAST(SPECIMEN_DATE AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(SPECIMEN_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3332' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_DATE' AS COL_NM 
    FROM YourSchemaName.SPECIMEN A
    WHERE  SPECIMEN_DATE IS NULL
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(SPECIMEN_DATE AS VARCHAR),1,30) AS ERR_VALUE, 'ID3333' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_DATE' AS COL_NM 
FROM YourSchemaName.SPECIMEN A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.DEATH B WHERE B.PERSON_ID = A.PERSON_ID
AND A.SPECIMEN_DATE < B.DEATH_DATE
)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( SPECIMEN_DATE IS NOT NULL AND DATALENGTH(CAST(SPECIMEN_DATE AS VARCHAR))>0 ) 
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
SELECT DISTINCT SUBSTRING(CAST(SPECIMEN_DATETIME AS VARCHAR),1,30) AS ERR_VALUE, 'ID3334' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_DATETIME' AS COL_NM 
FROM YourSchemaName.SPECIMEN
WHERE CASE WHEN CAST (SPECIMEN_DATETIME AS DATE) <> SPECIMEN_DATE
 THEN 'Y' 
 ELSE 'N' 
 END = 'Y' 
AND (SPECIMEN_DATETIME IS NOT NULL AND DATALENGTH(CAST(SPECIMEN_DATETIME AS VARCHAR))>0 )

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(QUANTITY AS VARCHAR),1,30) AS ERR_VALUE, 'ID3335' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'QUANTITY' AS COL_NM 
FROM YourSchemaName.SPECIMEN A
WHERE  QUANTITY IS NOT NULL AND DATALENGTH(CAST(QUANTITY AS VARCHAR)) > 0 
AND TRY_CAST(QUANTITY AS FLOAT) IS NULL

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(UNIT_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3336' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'UNIT_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.SPECIMEN A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'unit' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( UNIT_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(UNIT_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(ANATOMIC_SITE_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3337' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'ANATOMIC_SITE_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.SPECIMEN A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE 1=1 and lower(domain_id) = 'spec anatomic site' and lower(concept_class_id) = 'body structure' and lower(standard_concept) in ('standard', 's'))  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( ANATOMIC_SITE_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(ANATOMIC_SITE_CONCEPT_ID AS VARCHAR))>0 ) 

) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;

BEGIN TRY
    WITH query_result AS (
        SELECT TOP 3 ERR_VALUE, RULE_ID, TB_NM, COL_NM FROM (
 SELECT DISTINCT SUBSTRING(CAST(DISEASE_STATUS_CONCEPT_ID AS VARCHAR),1,30) AS ERR_VALUE, 'ID3338' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'DISEASE_STATUS_CONCEPT_ID' AS COL_NM 
FROM YourSchemaName.SPECIMEN A
WHERE CASE WHEN NOT EXISTS 
(SELECT 1 FROM YourSchemaName.CONCEPT B WHERE B.CONCEPT_ID = A.DISEASE_STATUS_CONCEPT_ID)  
THEN 'Y' ELSE 'N' END = 'Y' 
AND ( DISEASE_STATUS_CONCEPT_ID IS NOT NULL AND DATALENGTH(CAST(DISEASE_STATUS_CONCEPT_ID AS VARCHAR))>0 ) 
AND DISEASE_STATUS_CONCEPT_ID != 0
) XX

    )
    SELECT
        @RULE_ID_VAL = RULE_ID,
        @TB_NM_VAL = TB_NM,
        @COL_NM_VAL = COL_NM,
        @ERR_VALUE_VAL = ERR_VALUE
    FROM query_result;
    -- 결과를 테이블에 삽입
    UPDATE dbo.result_quality_check SET ERR_VALUE = @ERR_VALUE_VAL WHERE RULE_ID = @RULE_ID_VAL;
END TRY
BEGIN CATCH
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO dbo.quality_check_error_log (CHK_ID, ERROR_MESSAGE)
    VALUES (@RULE_ID_VAL, ERROR_MESSAGE());
END CATCH;
COMMIT;
 END; 


CREATE OR REPLACE PROCEDURE quality_check_tot_cnt()
LANGUAGE plpgsql
AS $$
DECLARE
    RULE_ID_VAL VARCHAR(30);
    TB_NM_VAL VARCHAR(50);
    COL_NM_VAL VARCHAR(50);
    TOT_CNT_VAL VARCHAR(50);
    ERR_CNT_VAL VARCHAR(50);
    ERR_VALUE_VAL TEXT;
BEGIN
    -- 테이블 생성
    CREATE TABLE IF NOT EXISTS result_quality_check (
        RULE_ID VARCHAR(30),
        TB_NM VARCHAR(50),
        COL_NM VARCHAR(50),
        TOT_CNT VARCHAR(50),
        ERR_CNT VARCHAR(50),
        ERR_VALUE TEXT
    );
   
   CREATE TABLE IF NOT EXISTS quality_check_error_log (
        SEQ SERIAL PRIMARY KEY,
        CHK_ID VARCHAR(30),
        CHK_TIME TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        ERROR_MESSAGE TEXT
    );
   
    BEGIN
        DELETE FROM result_quality_check WHERE 1=1;
        COMMIT;
    END;
BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1002' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1003' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1004' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1005' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID
WHERE A.PERSON_ID IS NOT NULL AND LENGTH(CAST(A.PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1006' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'GENDER_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE GENDER_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(GENDER_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1007' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'GENDER_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1008' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'YEAR_OF_BIRTH' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE YEAR_OF_BIRTH IS NOT NULL AND LENGTH(CAST(YEAR_OF_BIRTH AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1009' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'YEAR_OF_BIRTH' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1010' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'YEAR_OF_BIRTH' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE YEAR_OF_BIRTH IS NOT NULL AND LENGTH(CAST(YEAR_OF_BIRTH AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1011' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'MONTH_OF_BIRTH' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE MONTH_OF_BIRTH IS NOT NULL AND LENGTH(CAST(MONTH_OF_BIRTH AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1012' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'DAY_OF_BIRTH' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE DAY_OF_BIRTH IS NOT NULL AND LENGTH(CAST(DAY_OF_BIRTH AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1014' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'RACE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE RACE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(RACE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1015' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'RACE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1016' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'ETHNICITY_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE ETHNICITY_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(ETHNICITY_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1017' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'ETHNICITY_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1018' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'LOCATION_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE LOCATION_ID IS NOT NULL AND LENGTH(CAST(LOCATION_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;





BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1023' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'GENDER_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE GENDER_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(GENDER_SOURCE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1025' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'RACE_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE RACE_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(RACE_SOURCE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1027' AS RULE_ID,  
   'PERSON' AS TB_NM,    
   'ETHNICITY_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PERSON
WHERE ETHNICITY_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(ETHNICITY_SOURCE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1029' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1030' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1031' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1032' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
WHERE OBSERVATION_PERIOD_START_DATE IS NOT NULL AND LENGTH(CAST(OBSERVATION_PERIOD_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1033' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1034' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
WHERE OBSERVATION_PERIOD_START_DATE IS NOT NULL AND LENGTH(CAST(OBSERVATION_PERIOD_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1035' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.OBSERVATION_PERIOD_START_DATE IS NOT NULL AND LENGTH(CAST(A.OBSERVATION_PERIOD_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1036' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
WHERE OBSERVATION_PERIOD_END_DATE IS NOT NULL AND LENGTH(CAST(OBSERVATION_PERIOD_END_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1037' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1038' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'OBSERVATION_PERIOD_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.OBSERVATION_PERIOD_END_DATE IS NOT NULL AND LENGTH(CAST(A.OBSERVATION_PERIOD_END_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1039' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'PERIOD_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
WHERE PERIOD_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(PERIOD_TYPE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1040' AS RULE_ID,  
   'OBSERVATION_PERIOD' AS TB_NM,    
   'PERIOD_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION_PERIOD
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1042' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1043' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1044' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1045' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(VISIT_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1046' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1047' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_START_DATE IS NOT NULL AND LENGTH(CAST(VISIT_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1048' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1049' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_START_DATE IS NOT NULL AND LENGTH(CAST(VISIT_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1050' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.VISIT_START_DATE IS NOT NULL AND LENGTH(CAST(A.VISIT_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1051' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_START_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_START_DATETIME IS NOT NULL AND LENGTH(CAST(VISIT_START_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1052' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_END_DATE IS NOT NULL AND LENGTH(CAST(VISIT_END_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1053' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1054' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.VISIT_END_DATE IS NOT NULL AND LENGTH(CAST(A.VISIT_END_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1055' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_END_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_END_DATETIME IS NOT NULL AND LENGTH(CAST(VISIT_END_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1056' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(VISIT_TYPE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1057' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;




BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1061' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'VISIT_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE VISIT_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(VISIT_SOURCE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1062' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'ADMITTING_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE ADMITTING_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(ADMITTING_SOURCE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1064' AS RULE_ID,  
   'VISIT_OCCURRENCE' AS TB_NM,    
   'DISCHARGE_TO_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_OCCURRENCE
WHERE DISCHARGE_TO_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(DISCHARGE_TO_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;




BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1068' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1069' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1070' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1071' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(VISIT_DETAIL_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1072' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1073' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_START_DATE IS NOT NULL AND LENGTH(CAST(VISIT_DETAIL_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1074' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1075' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.VISIT_DETAIL_START_DATE IS NOT NULL AND LENGTH(CAST(A.VISIT_DETAIL_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1076' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL A JOIN YourSchemaName.VISIT_OCCURRENCE B ON B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID 
WHERE A.VISIT_DETAIL_START_DATE IS NOT NULL AND LENGTH(CAST(A.VISIT_DETAIL_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1077' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_START_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_START_DATETIME IS NOT NULL AND LENGTH(CAST(VISIT_DETAIL_START_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1078' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_END_DATE IS NOT NULL AND LENGTH(CAST(VISIT_DETAIL_END_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1079' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1080' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.VISIT_DETAIL_END_DATE IS NOT NULL AND LENGTH(CAST(A.VISIT_DETAIL_END_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1081' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL A JOIN YourSchemaName.VISIT_OCCURRENCE B ON B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID 
WHERE A.VISIT_DETAIL_END_DATE IS NOT NULL AND LENGTH(CAST(A.VISIT_DETAIL_END_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1082' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_END_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_END_DATETIME IS NOT NULL AND LENGTH(CAST(VISIT_DETAIL_END_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1083' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(VISIT_DETAIL_TYPE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1084' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;




BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1088' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_DETAIL_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_DETAIL_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(VISIT_DETAIL_SOURCE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1090' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'ADMITTING_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE ADMITTING_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(ADMITTING_SOURCE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1092' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'DISCHARGE_TO_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE DISCHARGE_TO_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(DISCHARGE_TO_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;



BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1095' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1096' AS RULE_ID,  
   'VISIT_DETAIL' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.VISIT_DETAIL
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1098' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1099' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1100' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1101' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(CONDITION_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1102' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1103' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(CONDITION_CONCEPT_ID AS VARCHAR))>0 
AND CONDITION_CONCEPT_ID IN (
SELECT DESCENDANT_CONCEPT_ID FROM YourSchemaName.CONCEPT_ANCESTOR WHERE ANCESTOR_CONCEPT_ID IN (45771080,4216771,3655355,4090861,4057948)
) INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1104' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(CONDITION_CONCEPT_ID AS VARCHAR))>0 
AND CONDITION_CONCEPT_ID IN (
SELECT DESCENDANT_CONCEPT_ID FROM YourSchemaName.CONCEPT_ANCESTOR WHERE ANCESTOR_CONCEPT_ID IN (4129486,4095793,443343,4268895,4056331)
) INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1105' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_START_DATE IS NOT NULL AND LENGTH(CAST(CONDITION_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1106' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1107' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_START_DATE IS NOT NULL AND LENGTH(CAST(CONDITION_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1108' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.CONDITION_START_DATE IS NOT NULL AND LENGTH(CAST(A.CONDITION_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1109' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE A JOIN YourSchemaName.VISIT_OCCURRENCE B ON B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID 
WHERE A.CONDITION_START_DATE IS NOT NULL AND LENGTH(CAST(A.CONDITION_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1110' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_START_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_START_DATETIME IS NOT NULL AND LENGTH(CAST(CONDITION_START_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;



BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1113' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(CONDITION_TYPE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1114' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1115' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'CONDITION_STATUS_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE CONDITION_STATUS_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(CONDITION_STATUS_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;



BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1118' AS RULE_ID,  
   'CONDITION_OCCURRENCE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.CONDITION_OCCURRENCE
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;






BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1124' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1125' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1126' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1127' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(DRUG_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1128' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1129' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_EXPOSURE_START_DATE IS NOT NULL AND LENGTH(CAST(DRUG_EXPOSURE_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1130' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1131' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_EXPOSURE_START_DATE IS NOT NULL AND LENGTH(CAST(DRUG_EXPOSURE_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1132' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.DRUG_EXPOSURE_START_DATE IS NOT NULL AND LENGTH(CAST(A.DRUG_EXPOSURE_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1133' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE A JOIN YourSchemaName.VISIT_OCCURRENCE B ON B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID 
WHERE A.DRUG_EXPOSURE_START_DATE IS NOT NULL AND LENGTH(CAST(A.DRUG_EXPOSURE_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1134' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_START_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_EXPOSURE_START_DATETIME IS NOT NULL AND LENGTH(CAST(DRUG_EXPOSURE_START_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1135' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_EXPOSURE_END_DATE IS NOT NULL AND LENGTH(CAST(DRUG_EXPOSURE_END_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1136' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1137' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_EXPOSURE_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.DRUG_EXPOSURE_END_DATE IS NOT NULL AND LENGTH(CAST(A.DRUG_EXPOSURE_END_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;



BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1140' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(DRUG_TYPE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1141' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;



BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1144' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'QUANTITY' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE QUANTITY IS NOT NULL AND LENGTH(CAST(QUANTITY AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1145' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DAYS_SUPPLY' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DAYS_SUPPLY IS NOT NULL AND LENGTH(CAST(DAYS_SUPPLY AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1147' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'ROUTE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE ROUTE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(ROUTE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;



BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1150' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;



BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1153' AS RULE_ID,  
   'DRUG_EXPOSURE' AS TB_NM,    
   'DRUG_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DRUG_EXPOSURE
WHERE DRUG_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(DRUG_SOURCE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;




BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1157' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1158' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1159' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1160' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(PROCEDURE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1161' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1162' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_DATE IS NOT NULL AND LENGTH(CAST(PROCEDURE_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1163' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1164' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_DATE IS NOT NULL AND LENGTH(CAST(PROCEDURE_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1165' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.PROCEDURE_DATE IS NOT NULL AND LENGTH(CAST(A.PROCEDURE_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1166' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE A JOIN YourSchemaName.VISIT_OCCURRENCE B ON B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID 
WHERE A.PROCEDURE_DATE IS NOT NULL AND LENGTH(CAST(A.PROCEDURE_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1167' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_DATETIME IS NOT NULL AND LENGTH(CAST(PROCEDURE_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1168' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE PROCEDURE_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(PROCEDURE_TYPE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1169' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'PROCEDURE_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1170' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'MODIFIER_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE MODIFIER_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(MODIFIER_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1171' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'QUANTITY' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE QUANTITY IS NOT NULL AND LENGTH(CAST(QUANTITY AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1173' AS RULE_ID,  
   'PROCEDURE_OCCURRENCE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.PROCEDURE_OCCURRENCE
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;






BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1179' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1180' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1181' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1182' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1183' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(DEVICE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1184' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1185' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_EXPOSURE_START_DATE IS NOT NULL AND LENGTH(CAST(DEVICE_EXPOSURE_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1186' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1187' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.DEVICE_EXPOSURE_START_DATE IS NOT NULL AND LENGTH(CAST(A.DEVICE_EXPOSURE_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1188' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE A JOIN YourSchemaName.VISIT_OCCURRENCE B ON B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID 
WHERE A.DEVICE_EXPOSURE_START_DATE IS NOT NULL AND LENGTH(CAST(A.DEVICE_EXPOSURE_START_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1189' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_START_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_EXPOSURE_START_DATETIME IS NOT NULL AND LENGTH(CAST(DEVICE_EXPOSURE_START_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1190' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_EXPOSURE_END_DATE IS NOT NULL AND LENGTH(CAST(DEVICE_EXPOSURE_END_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1191' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_END_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.DEVICE_EXPOSURE_END_DATE IS NOT NULL AND LENGTH(CAST(A.DEVICE_EXPOSURE_END_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1192' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_EXPOSURE_END_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_EXPOSURE_END_DATETIME IS NOT NULL AND LENGTH(CAST(DEVICE_EXPOSURE_END_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1193' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(DEVICE_TYPE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1194' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1196' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'QUANTITY' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE QUANTITY IS NOT NULL AND LENGTH(CAST(QUANTITY AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1198' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;



BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1201' AS RULE_ID,  
   'DEVICE_EXPOSURE' AS TB_NM,    
   'DEVICE_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEVICE_EXPOSURE
WHERE DEVICE_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(DEVICE_SOURCE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1203' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1204' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1205' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1206' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE MEASUREMENT_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(MEASUREMENT_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1207' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1208' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE MEASUREMENT_DATE IS NOT NULL AND LENGTH(CAST(MEASUREMENT_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1209' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1210' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE MEASUREMENT_DATE IS NOT NULL AND LENGTH(CAST(MEASUREMENT_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1211' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.MEASUREMENT_DATE IS NOT NULL AND LENGTH(CAST(A.MEASUREMENT_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1212' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT A JOIN YourSchemaName.VISIT_OCCURRENCE B ON B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID 
WHERE A.MEASUREMENT_DATE IS NOT NULL AND LENGTH(CAST(A.MEASUREMENT_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1213' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE MEASUREMENT_DATETIME IS NOT NULL AND LENGTH(CAST(MEASUREMENT_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1215' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE MEASUREMENT_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(MEASUREMENT_TYPE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1216' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'MEASUREMENT_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1217' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'OPERATOR_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE OPERATOR_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(OPERATOR_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1218' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'VALUE_AS_NUMBER' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE VALUE_AS_NUMBER IS NOT NULL AND LENGTH(CAST(VALUE_AS_NUMBER AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1219' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'VALUE_AS_NUMBER' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1220' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'VALUE_AS_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE VALUE_AS_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(VALUE_AS_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1221' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'UNIT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE UNIT_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(UNIT_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1222' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'RANGE_LOW' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE RANGE_LOW IS NOT NULL AND LENGTH(CAST(RANGE_LOW AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1223' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'RANGE_HIGH' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE RANGE_HIGH IS NOT NULL AND LENGTH(CAST(RANGE_HIGH AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1225' AS RULE_ID,  
   'MEASUREMENT' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.MEASUREMENT
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;







BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1232' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1233' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1234' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1235' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(OBSERVATION_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1236' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1237' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_DATE IS NOT NULL AND LENGTH(CAST(OBSERVATION_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1238' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1239' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_DATE IS NOT NULL AND LENGTH(CAST(OBSERVATION_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1240' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.OBSERVATION_DATE IS NOT NULL AND LENGTH(CAST(A.OBSERVATION_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1241' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION A JOIN YourSchemaName.VISIT_OCCURRENCE B ON B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID 
WHERE A.OBSERVATION_DATE IS NOT NULL AND LENGTH(CAST(A.OBSERVATION_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1242' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_DATETIME IS NOT NULL AND LENGTH(CAST(OBSERVATION_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1243' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(OBSERVATION_TYPE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1244' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1245' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'VALUE_AS_NUMBER' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE VALUE_AS_NUMBER IS NOT NULL AND LENGTH(CAST(VALUE_AS_NUMBER AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1246' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'VALUE_AS_NUMBER' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1248' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'VALUE_AS_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE VALUE_AS_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(VALUE_AS_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1249' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'QUALIFIER_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE QUALIFIER_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(QUALIFIER_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1250' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'UNIT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE UNIT_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(UNIT_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1252' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;



BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1255' AS RULE_ID,  
   'OBSERVATION' AS TB_NM,    
   'OBSERVATION_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.OBSERVATION
WHERE OBSERVATION_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(OBSERVATION_SOURCE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;



BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1258' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1259' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1260' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH A JOIN YourSchemaName.PERSON B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.PERSON_ID IS NOT NULL AND LENGTH(CAST(A.PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1261' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'DEATH_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH
WHERE DEATH_DATE IS NOT NULL AND LENGTH(CAST(DEATH_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1262' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'DEATH_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1264' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'DEATH_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH
WHERE DEATH_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(DEATH_TYPE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1265' AS RULE_ID,  
   'DEATH' AS TB_NM,    
   'CAUSE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.DEATH
WHERE CAUSE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(CAUSE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;




BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1269' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1270' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1271' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1272' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE NOTE_DATE IS NOT NULL AND LENGTH(CAST(NOTE_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1273' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1274' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.NOTE_DATE IS NOT NULL AND LENGTH(CAST(A.NOTE_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1275' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE A JOIN YourSchemaName.VISIT_OCCURRENCE B ON B.VISIT_OCCURRENCE_ID = A.VISIT_OCCURRENCE_ID 
WHERE A.NOTE_DATE IS NOT NULL AND LENGTH(CAST(A.NOTE_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1276' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE NOTE_DATETIME IS NOT NULL AND LENGTH(CAST(NOTE_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1277' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE NOTE_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(NOTE_TYPE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1278' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1279' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_CLASS_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE NOTE_CLASS_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(NOTE_CLASS_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1280' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_CLASS_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;



BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1283' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'NOTE_TEXT' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1284' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'ENCODING_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE ENCODING_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(ENCODING_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1285' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'ENCODING_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1286' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'LANGUAGE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE LANGUAGE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(LANGUAGE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1287' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'LANGUAGE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1289' AS RULE_ID,  
   'NOTE' AS TB_NM,    
   'VISIT_OCCURRENCE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE
WHERE VISIT_OCCURRENCE_ID IS NOT NULL AND LENGTH(CAST(VISIT_OCCURRENCE_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;




BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1293' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_NLP_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1294' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
WHERE NOTE_ID IS NOT NULL AND LENGTH(CAST(NOTE_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1295' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1296' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'SECTION_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
WHERE SECTION_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(SECTION_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;




BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1300' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'LEXICAL_VARIANT' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1301' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_NLP_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
WHERE NOTE_NLP_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(NOTE_NLP_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1302' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NOTE_NLP_SOURCE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
WHERE NOTE_NLP_SOURCE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(NOTE_NLP_SOURCE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;


BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1304' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NLP_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
WHERE NLP_DATE IS NOT NULL AND LENGTH(CAST(NLP_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1305' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NLP_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1306' AS RULE_ID,  
   'NOTE_NLP' AS TB_NM,    
   'NLP_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.NOTE_NLP
WHERE NLP_DATETIME IS NOT NULL AND LENGTH(CAST(NLP_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;





BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1311' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1312' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE PERSON_ID IS NOT NULL AND LENGTH(CAST(PERSON_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1313' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'PERSON_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1314' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE SPECIMEN_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(SPECIMEN_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1315' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1316' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE SPECIMEN_TYPE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(SPECIMEN_TYPE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1317' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_TYPE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1318' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE SPECIMEN_DATE IS NOT NULL AND LENGTH(CAST(SPECIMEN_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1319' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
 INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1320' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_DATE' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN A JOIN YourSchemaName.DEATH B ON B.PERSON_ID = A.PERSON_ID 
WHERE A.SPECIMEN_DATE IS NOT NULL AND LENGTH(CAST(A.SPECIMEN_DATE AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1321' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'SPECIMEN_DATETIME' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE SPECIMEN_DATETIME IS NOT NULL AND LENGTH(CAST(SPECIMEN_DATETIME AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1322' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'QUANTITY' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE QUANTITY IS NOT NULL AND LENGTH(CAST(QUANTITY AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1323' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'UNIT_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE UNIT_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(UNIT_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1324' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'ANATOMIC_SITE_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE ANATOMIC_SITE_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(ANATOMIC_SITE_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;     COMMIT;

BEGIN
    -- 쿼리 결과를 변수에 저장
    SELECT 'ID1325' AS RULE_ID,  
   'SPECIMEN' AS TB_NM,    
   'DISEASE_STATUS_CONCEPT_ID' AS COL_NM, 
   COUNT(*) AS TOT_CNT   
   FROM YourSchemaName.SPECIMEN
WHERE DISEASE_STATUS_CONCEPT_ID IS NOT NULL AND LENGTH(CAST(DISEASE_STATUS_CONCEPT_ID AS VARCHAR))>0  INTO RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL;
    -- 결과를 테이블에 삽입
    INSERT INTO result_quality_check (RULE_ID, TB_NM, COL_NM, TOT_CNT)
    VALUES (RULE_ID_VAL, TB_NM_VAL, COL_NM_VAL, TOT_CNT_VAL);
EXCEPTION WHEN OTHERS THEN
    -- 오류 발생 시 에러 로그에 기록
    INSERT INTO quality_check_error_log(CHK_ID, error_message)
    VALUES (RULE_ID_VAL, SQLERRM);
END;
    COMMIT;
END;
$$;
